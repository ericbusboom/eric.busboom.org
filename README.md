# eric.busboom.org

Static Hugo site converted from WordPress.

## Development
```bash
just dev    # or: hugo server --buildDrafts
just build  # or: hugo --minify --buildDrafts
```

## Deployment
Pushes to `main` trigger GitHub Actions → GitHub Pages.
